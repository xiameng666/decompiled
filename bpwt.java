import j..util.Collection.-EL;
import j..util.function.Function.-CC;
import java.util.List;

public final class bpwt implements gfsi {
    @Override  // gfsi
    public final Object apply(Object object0) {
        return gged_interceptors.i(((ggeo)Collection.-EL.stream(((List)object0)).filter(new bpwn()).collect(ggaf.c(new bpwo(), Function.-CC.identity(), new bpwp()))).g());
    }
}

