import android.content.Intent;

public final class dpcg implements dpdi {
    @Override  // dpdi
    public final du a(Intent intent0) {
        ibuq.f(intent0, "intent");
        du du0 = new eeqk();
        du0.setArguments(intent0.getExtras());
        return du0;
    }
}

