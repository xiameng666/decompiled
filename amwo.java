import j..util.Collection.-EL;
import j..util.stream.Collectors;
import java.util.List;

public final class amwo implements gfsi {
    @Override  // gfsi
    public final Object apply(Object object0) {
        return (ggfp)Collection.-EL.stream(((List)object0)).map(new amwi()).collect(Collectors.collectingAndThen(Collectors.toList(), new amwj()));
    }
}

