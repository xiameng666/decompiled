import android.app.KeyguardManager;
import android.content.Context;

public final class ajjh {
    public static final ajji a(Context context0) {
        return new ajji(context0, new ajjm(((KeyguardManager)context0.getSystemService("keyguard"))), new ajjo(context0), new bhnc(context0));
    }
}

