import android.accounts.Account;
import j..time.Duration;

public interface boxs {
    public static final Duration a;

    static {
        boxs.a = Duration.ofDays(2L);
    }

    gmcd a();

    gmcd b(Account arg1, boolean arg2, int arg3, int arg4, gfsx arg5);
}

