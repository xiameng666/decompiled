import android.content.Intent;
import com.google.android.gms.pay.IntentSource;
import j..util.Objects;

public final class dozt implements dpdi {
    @Override  // dpdi
    public final du a(Intent intent0) {
        IntentSource intentSource0 = ednn.a(intent0.getExtras()).d;
        return intentSource0 != null && ((IntentSource)Objects.requireNonNull(intentSource0)).a == 20 ? eess.E(intent0) : eexu.I(intent0);
    }
}

