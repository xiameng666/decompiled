import java.util.Comparator;

final class wzh implements Comparator {
    @Override
    public final int compare(Object object0, Object object1) {
        return ((xev)object0).c.compareTo(((xev)object1).c);
    }
}

