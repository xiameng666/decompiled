import java.util.Locale;

public final class cpah {
    public static final boolean a(String s) {
        for(Object object0: hvbr.a.f().l().b) {
            if(s.toLowerCase(Locale.US).contains(((String)object0).toLowerCase(Locale.US))) {
                return true;
            }
        }
        return false;
    }
}

