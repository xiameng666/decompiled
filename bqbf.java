import java.util.Map.Entry;

public final class bqbf implements gfsi {
    @Override  // gfsi
    public final Object apply(Object object0) {
        hldp hldp0 = (hldp)((Map.Entry)object0).getKey();
        return (hldp0.b & 0x40) != 0 ? (hldp0.i == null ? hldk.a : hldp0.i).c : null;
    }
}

