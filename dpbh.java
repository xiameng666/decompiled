import android.content.Intent;
import com.google.android.gms.pay.settings.verification.transit.wear.WearTransitSettingsFragment;

public final class dpbh implements dpdi {
    @Override  // dpdi
    public final du a(Intent intent0) {
        ibuq.f(intent0, "intent");
        du du0 = new WearTransitSettingsFragment();
        du0.setArguments(intent0.getExtras());
        return du0;
    }
}

