public enum dctf {
    SUCCESS,
    SIGN_IN_REQUIRE,
    ALREADY_EXIST,
    ALREADY_STARTED,
    NETWORK_ERROR,
    INVALID_ARGUMENT,
    REMOTE_ERROR,
    FILE_LIMIT_EXCEED_COUNT,
    FILE_LIMIT_EXCEED_SIZE;

    static {
        dctf.j = arr_dctf;
        ibsn.a(arr_dctf);
    }
}

