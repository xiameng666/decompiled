import android.util.Pair;
import java.security.PublicKey;

public interface blxk {
    int a();

    Pair b(PublicKey arg1);

    byte[] c(byte[] arg1, byte[] arg2);

    byte[] d(byte[] arg1, byte[] arg2);

    byte[] e(byte[] arg1, byte[] arg2);
}

