import j..time.Duration;
import j..util.Optional;

public final class eoit implements gfsi {
    @Override  // gfsi
    public final Object apply(Object object0) {
        return (Duration)((Optional)object0).map(new eoiu()).orElse(Duration.ZERO);
    }
}

