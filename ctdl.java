public enum ctdl {
    UNKNOWN_SOURCE,
    SETTINGS,
    DEVICE_GROUP,
    ENROLLMENT_CHECKER,
    SETTINGS_OPT_IN,
    INVITATION;

    static {
        ctdl.g = arr_ctdl;
        ibsn.a(arr_ctdl);
    }
}

