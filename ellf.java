import java.io.IOException;

public final class ellf implements glzn {
    @Override  // glzn
    public final gmcd a(Object object0) {
        return ((elim)object0) == null || ((elim)object0).g.size() == 0 ? gmbu.h(new IOException("No key available from protoStore")) : gmbu.i(((elim)object0));
    }
}

