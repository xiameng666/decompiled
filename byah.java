public enum byah {
    STATUS_UNKNOWN,
    SUCCESS,
    FAILURE_APP_CANCELLED,
    FAILURE_USER_CANCELLED,
    FAILURE_INTERNAL_ERROR,
    FAILURE_PROVIDER_ERROR,
    FAILURE_NO_CREDENTIAL,
    CANCELLED_FOR_SETTINGS;

    static {
        byah.i = arr_byah;
        ibsn.a(arr_byah);
    }
}

