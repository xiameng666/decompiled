import java.lang.reflect.Constructor;

public final class mxx implements mxy {
    @Override  // mxy
    public final Constructor a() {
        return Class.forName("androidx.media3.decoder.midi.MidiExtractor").asSubclass(mye.class).getConstructor(null);
    }
}

