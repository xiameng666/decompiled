import java.util.Comparator;

public final class ble implements Comparator {
    @Override
    public final int compare(Object object0, Object object1) {
        return ((bjr)object0).a.compareTo(((bjr)object1).a);
    }
}

