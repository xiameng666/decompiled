public enum fmav {
    VALID(0),
    INVALID(1);

    public final int c;

    private fmav(int v1) {
        this.c = v1;
    }

    public static fmav a(int v) {
        return (fmav)ggch.k(fmav.values()).c(new fmau(v)).f(fmav.a);
    }
}

