import java.util.regex.Pattern;

public final class fbrf implements gful_cronetEngineProvider {
    @Override  // gful_cronetEngineProvider
    public final Object mr() {
        return Pattern.compile(hzmx.a.b().a());
    }
}

