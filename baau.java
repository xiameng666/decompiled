import java.util.Random;
import java.util.concurrent.ScheduledExecutorService;

public final class baau implements Runnable {
    @Override
    public final void run() {
        bbit bbit0 = null;
        if(bbiy.a && hryz.d()) {
            ScheduledExecutorService scheduledExecutorService0 = clhl.b.h(1, clhr.b);
            Random random0 = new Random();
            random0.nextLong();
            random0.nextLong();
            bbit0 = new bbit(scheduledExecutorService0, new bbiq(random0));
        }
        bbiy.b = bbit0;
        esaa.b = new bbix(bbit0);
    }
}

