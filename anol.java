import android.util.Pair;
import java.util.concurrent.TimeUnit;

public final class anol implements gful_cronetEngineProvider {
    @Override  // gful_cronetEngineProvider
    public final Object mr() {
        return Pair.create(Long.valueOf(hqep.a.g().e()), TimeUnit.MILLISECONDS);
    }
}

