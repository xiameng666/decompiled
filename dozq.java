import android.content.Intent;

public final class dozq implements dpdi {
    @Override  // dpdi
    public final du a(Intent intent0) {
        ibuq.f(intent0, "intent");
        du du0 = new drrj();
        du0.setArguments(intent0.getExtras());
        return du0;
    }
}

