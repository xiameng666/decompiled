import java.util.regex.Pattern;

public final class fbre implements gful_cronetEngineProvider {
    @Override  // gful_cronetEngineProvider
    public final Object mr() {
        return Pattern.compile("[0-9]*(\\.[0-9][0-9]?)?");
    }
}

