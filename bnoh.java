import j..util.Collection.-EL;

public final class bnoh implements gfsi {
    @Override  // gfsi
    public final Object apply(Object object0) {
        return (gged_interceptors)Collection.-EL.stream(((gged_interceptors)object0)).map(new bnog()).collect(ggaf.a);
    }
}

