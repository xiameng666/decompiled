import android.app.PendingIntent;

public final class edzn implements ibts {
    @Override  // ibts
    public final Object a(Object object0) {
        PendingIntent pendingIntent0 = (PendingIntent)object0;
        ibuq.f(pendingIntent0, "pendingIntent");
        pendingIntent0.send();
        return ibom.a;
    }
}

