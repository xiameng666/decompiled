public final class tdf extends tdd {
    public static final tcz f;

    static {
        tdf.f = new tcz("Amount, Other", "Secondary amount associated with the transaction representing a cashback amount", tdf.c, 6, 0x7FFFFFFF, 1, 2, 0, 1, 1, tdf.d, 1);
    }

    public tdf(byte[] arr_b) {
        super(arr_b, tdf.f);
    }
}

