import java.security.PublicKey;
import java.security.Signature;

interface blqr {
    PublicKey a(blzl arg1, byte[] arg2);

    Signature b(blzl arg1, byte[] arg2);

    void c(blzl arg1);

    byte[] d(blzl arg1, boolean arg2, boolean arg3, byte[] arg4);

    bmbs e(byte[] arg1);
}

